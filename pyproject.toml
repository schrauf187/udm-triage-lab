[project]
name = "udm-triage-lab"
version = "0.1.0"
description = "AI-assisted SOC alert triage and UDM normalization MVP (Streamlit)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.116.0",
    "pandas>=3.0.3",
    "requests>=2.34.2",
    "streamlit>=1.59.0",
]

[tool.uv]
package = false
