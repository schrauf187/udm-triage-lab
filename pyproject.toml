[project]
name = "udm-triage-lab"
version = "0.1.0"
description = "AI-assisted SOC alert triage and UDM normalization MVP (Streamlit)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.115.1",
    "google-auth>=2.55.1",
    "gspread>=6.2.1",
    "pandas>=3.0.3",
    "requests>=2.32",
    "streamlit>=1.58.0",
]

[tool.uv]
package = false
