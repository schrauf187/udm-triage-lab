[project]
name = "udm-triage-lab"
version = "0.1.0"
description = "AI-assisted SOC alert triage and UDM normalization MVP (Streamlit)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.118.0",
    "google-auth>=2.56.2",
    "gspread>=6.2.1",
    "pandas>=3.0.5",
    "requests>=2.34.2",
    "streamlit>=1.60.0",
]

[tool.uv]
package = false
